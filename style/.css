
    .navbar {
    background: #000;
    position: sticky;
    color:#fff;
    display:block;
    padding:1rem;
    }
    a {
      text-decoration:none;
    }
    body {
      margin:0;
    }
    .nav-brand {
      font-size:2rem;
      color:#cccccc;
    }
    .row {
      display:flex;
      flex-wrap: wrap;
    }
    @media (min-width: 992px){
      .nav-right {
        flex: 0 0 25%;
        max-width: 25%;
        text-align: right;
      }
      .nav-left {
        flex: 0 0 75%;
        max-width: 75%;
      }}
